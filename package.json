{
  "name": "amr-dlms-tcp-server",
  "version": "1.0.0",
  "private": true,
  "description": "TCP ingestion server for newline-delimited JSON packets from ESP32 AMR devices",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/line-framer.test.js test/server.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED"
}
